# Copy to .env (gitignored). Use a FRESHLY-ROTATED OpenAI key — never paste keys into chat.
OPENAI_API_KEY=sk-...
# Local CF Worker (ENS tools + /evaluate). Run `bun run worker:dev` in the repo root.
WORKER_URL=http://127.0.0.1:8787
WORKER_API_KEY=local-dev-key
# Only needed for transfer_execute (BYOK signing unlock). Reads don't need it.
MM_PASSWORD=
